[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short -m "not integration"
markers =
    integration: marks tests requiring a live Ollama instance (deselect with '-m "not integration"')
